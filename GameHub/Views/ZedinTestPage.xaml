<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GameHub.Views.ZedinTestPage"
             Title="Монеточка"
             xmlns:modules="clr-namespace:GameHub.Views.Modules" BackgroundColor="#98A9B9">
    <ScrollView>
        <Grid >
            <Grid.ColumnDefinitions>

                <ColumnDefinition Width="*"/>

                <ColumnDefinition Width="8*"/>

                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Border Padding="40" Grid.Column="1" Grid.Row="0" VerticalOptions="Center" BackgroundColor="White" Stroke="White" StrokeShape="RoundRectangle 10" Margin="10">
                <VerticalStackLayout >
                    <modules:Coin/>
                </VerticalStackLayout>
            </Border>
        </Grid>
    </ScrollView>
</ContentPage>